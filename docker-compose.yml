version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: adim
      POSTGRES_PASSWORD: adim
      POSTGRES_DB: adim
    ports:
      - "5432:5432"

  backend:
    build: .
    depends_on:
      - db
    ports:
      - "8000:8000"